# GENERATED FILE - DO NOT EDIT.
# Rendered from charts/macroshock by scripts/render_k8s.py; edit the chart instead.
# Regenerate with:  python scripts/render_k8s.py
# Source: macroshock/templates/dashboard-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: macroshock-dashboard
  labels:
    helm.sh/chart: macroshock-0.1.0
    app.kubernetes.io/name: macroshock
    app.kubernetes.io/instance: macroshock
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: macroshock
    app.kubernetes.io/version: "4.0.0"
    app.kubernetes.io/component: dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: macroshock
      app.kubernetes.io/instance: macroshock
      app.kubernetes.io/component: dashboard
  template:
    metadata:
      labels:
        app.kubernetes.io/name: macroshock
        app.kubernetes.io/instance: macroshock
        app.kubernetes.io/component: dashboard
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: dashboard
          image: "ghcr.io/vaishnavi-eklaspur/macroshock-dashboard:latest"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: false
          ports:
            - name: http
              containerPort: 8501
              protocol: TCP
          # The dashboard builds the covariance/regime model in-process on first start.
          startupProbe:
            httpGet:
              path: /_stcore/health
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /_stcore/health
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /_stcore/health
              port: http
            periodSeconds: 20
          resources:
            limits:
              cpu: "1"
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 512Mi

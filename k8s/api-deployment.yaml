# GENERATED FILE - DO NOT EDIT.
# Rendered from charts/macroshock by scripts/render_k8s.py; edit the chart instead.
# Regenerate with:  python scripts/render_k8s.py
# Source: macroshock/templates/api-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: macroshock-api
  labels:
    helm.sh/chart: macroshock-0.1.0
    app.kubernetes.io/name: macroshock
    app.kubernetes.io/instance: macroshock
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: macroshock
    app.kubernetes.io/version: "4.0.0"
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: macroshock
      app.kubernetes.io/instance: macroshock
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: macroshock
        app.kubernetes.io/instance: macroshock
        app.kubernetes.io/component: api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "5000"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: api
          image: "ghcr.io/vaishnavi-eklaspur/macroshock-api:latest"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: false
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          env:
            - name: MACROSHOCK_CSV
              value: "data/real_asset_returns.csv"
            - name: MACROSHOCK_DB
              value: "/app/dbdata/macroshock.db"
            - name: MACROSHOCK_FACTORS_CSV
              value: "data/real_factor_returns.csv"
            - name: MACROSHOCK_SOURCE
              value: "csv"
          # The API seeds its store on start, so give it room before liveness bites.
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 20
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi

# Build-context trim for the dashboard image (built from the repo root). The Dockerfile only
# COPYs backend/ and frontend/, so everything else is excluded: a smaller, faster context, and
# a guarantee that nothing incidental is ever baked into a published image.
.*
docs
frontend-react
deploy
charts
k8s
cli
scripts
results
**/__pycache__
**/*.py[cod]
.venv
venv
env
*.db
*.sqlite
*.sqlite3
htmlcov
.DS_Store
